# 验证记录

## 自动测试

`node --test tests/*.test.mjs`：28 项通过，保留原有 19 项场景并新增 9 项。

`actionlint 1.7.12 -shellcheck= -pyflakes= .github/workflows/update-market-data.yml`：通过。脚本与前端 JS 语法检查、`git diff --check` 通过。

- 新增：静态 parser（四个现有读取函数）、单次下载复用、no-store、缺 Secret 明确报错。
- 新增：上游失败保留旧值/原时间、其他标的继续、必要时回退、同 UTC 日累计 25 次上限。
- 新增：新鲜快照重复运行不写文件、请求前保存预算、白名单排除密钥与个人字段。
- 新增：旧配置默认迁移 Actions 但保留个人 Key/URL/交易，Pages 与 raw 副本选择较新版本。

- v1 合并迁移保留持仓、交易、API Key、自定义设置，并补默认里程碑。
- 无法解析的原有数据禁止写回默认值；原始内容可导出。
- 未来月度预测、季度历史锚点、未知月份留白、清仓后预测归零。
- 公告替换派生预测，年度已收+宣布+预测合计正确；派生预测不写入 localStorage 交易。
- 六个里程碑逐个测试阈值上下边界，美元显示仍按人民币解锁。
- 无记录删除、有记录取消/确认、级联删除、备份失败中止、异步返回不产生孤儿股息。
- 已收到股息的净金额不会被公告更新覆盖。
- 逐资源新鲜度、失败退避、可重复自动刷新、每日请求预算、超限停止回退请求。
- 弹窗/设置表单不被后台完成渲染替换，缺失价格不显示完整市值。
- 像素柱高且比例不受巨大月目标压缩。
- 共享服务并发去重、重启后保留预算、CORS、静态文件白名单、未配置密钥时明确失败。
- 共享服务不接收个人 Key，保留上游缓存获取时间。

`node tests/browser.mjs`：通过，使用 Chromium 与 WebKit 回归。

- Chromium：390、430、1280px 首页无横向溢出；390px 组合无横向溢出。
- 12 个月 DOM 与实际非零柱高；首页在一个 100dvh App 页面中展示紧凑里程碑进度与年度趋势。
- 底部固定导航严格为首页、持仓、日历、设置四项；新增 FAB 仅出现在前三页且位于导航栏上方。
- 首页、持仓、日历、设置在 Chromium / WebKit 的 390px、430px 下均无 document 纵向滚动与横向溢出。
- 持仓仅保留紧凑核心行，列表内部滚动；详情、编辑与删除置于 Bottom Sheet。日历固定月历和所选日期事件区；设置采用入口行和 Bottom Sheet。
- 新增标的、无记录删除、有记录取消和确认删除、备份、刷新后数据保留。
- 自定义里程碑设置保存与刷新持久化。
- Chromium 离线刷新后应用可用，缓存名 tangping-dividend-v7.3。
- 受控旧缓存升级到 v7.3，观测到且仅观测到一次自动页面刷新。
- WebKit：390px 图表非零柱子可见，无横向溢出。
- Chromium / WebKit：无个人 Key 自动读取快照、网络新快照覆盖旧值、离线及 HTTP 503 回退最近成功快照，均通过。
- 正常功能测试没有捕获到 JavaScript pageerror。
- 已查看浅色、深色、首页、图表和组合截图；右侧刻度与柱子分离，手机卡片删除可见。

## 限制

- WebKit 自动化不等于真实 iPhone 主屏幕安装实测；离线与升级测试在本地浏览器环境执行。
- 未使用用户真实数据或个人密钥进行外部行情请求；行情边界通过注入响应验证。
- Windows WebKit 离线顶层导航存在运行时 internal error，因此 WebKit 测试验证离线快照请求，不声称完成其离线整页重载；Chromium 完成了离线整页重载。
- 未配置/读取真实行情 Secret，未运行线上定时任务。本次提交的快照为空初始化文件，PR 合并并配置 Secret 后真实生成；无需 Node 后台。
- 所有截图中 DEMO 资产及金额均为独立测试夹具，不是用户持仓，不写入产品默认数据。
